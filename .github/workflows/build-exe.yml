name: build-exe

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements-dev.txt
      # --onedir (pas --onefile) : un exécutable "onefile" se ré-extrait dans
      # un dossier temporaire à chaque lancement (démarrage lent, plus souvent
      # bloqué par l'antivirus). --onedir démarre directement depuis le
      # disque. Interface en tkinter : aucune dépendance native à empaqueter.
      - run: >
          pyinstaller
          --onedir
          --windowed
          --name QGIS-LTR-Updater
          main.py
      - uses: actions/upload-artifact@v4
        with:
          name: QGIS-LTR-Updater-windows
          path: dist/QGIS-LTR-Updater/
          retention-days: 30
